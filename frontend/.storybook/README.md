# Storybook Config

Storybook setup for previewing UI components in isolation.

## Contents
- `main.ts` – Storybook framework and build configuration.
- `preview.ts` – global decorators, parameters, and styles.
- `vitest.setup.ts` – Storybook project annotations and Next navigation mocks for component tests.
